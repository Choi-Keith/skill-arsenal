---
description: Idiomatic Go guide — concurrency with goroutines/channels, generics, interfaces, error handling, gRPC/REST microservices, pprof performance optimization, and testing
argument-hint: "<task or question>"
---

# /go -- Golang Engineering Guide

Apply the `dev-golang` skill to: $ARGUMENTS

Covers concurrent patterns with goroutines and channels, microservices with gRPC or REST, performance optimization with pprof and benchmarks, and idiomatic Go with generics, interfaces, and robust error handling.

Load the matching topic file from the skill's `references/` (e.g. `concurrency.md`, `generics.md`, `interfaces.md`, `project-structure.md`, `testing.md`) before answering in depth.
