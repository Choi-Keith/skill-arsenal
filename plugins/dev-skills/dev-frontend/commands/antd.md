---
description: Ant Design decision guide — component selection, theming/tokens, SSR, a11y, performance, Pro/ProComponents, Ant Design X, migration, and local API lookup via @ant-design/cli
argument-hint: "<question, component, or scenario>"
---

# /antd -- Ant Design Decision Guide

Apply the `dev-antd` skill to answer or implement: $ARGUMENTS

Covers antd 6.x, Ant Design Pro 5/ProComponents, Ant Design X v2, and the offline `@ant-design/cli` — component selection, theming/tokens, SSR, accessibility, performance, routing/access/CRUD, AI/chat UI patterns, local API lookup, debugging, migration, and usage analysis.

For exact component APIs, prefer the offline lookup workflow described in the skill's `references/antd-cli.md` instead of relying on memory.
